# miscellaneous
1. notebooks中的文件用来画图，研究

# code
1. 修改logger.py类
2. 将代码中所有输出log的地方全部使用logger.py打印信息
3. csv读取，存储相关的代码进行封装，还有解压，合并，压缩的代码也进行封装
4. 将常见的指标放在metrics文件中，比如说回测计算夏普率的等等
5. 保持常用的脚本不用经常修改代码，比如下载数据的脚本，可以通过命令行传递参数，也可以使用configs文件夹下的配置传递参数（还是使用configs文件夹传递参数比较好）
6. 看看有没有必要搞一个画图的类


# insight
## data
1. 对threshold做ema的作用 
- 一开始对过去threshold的ema本质上是为了捕捉市场长期的偏移，比如说2020年-2025年，市场有一个缓慢变热的一个趋势，那么用ema是可以捕捉到的
- 但是，想要用过去的threshold对未来进行预测是不可能的，简直跟预测价格一样。过去发生的事情不代表未来就会发生。
2. 因此，有两种方法
- 第一种是**让threshold变平滑一点**，这是可以做到的
- 第二种，




1. bar的生成数量是非平稳的，有没有方法让他变得平稳
-> 当然是有办法的，那就是 
$$ \frac{今天生成bar的数量 - 昨天生成bar的数量}{今天生成bar的数量} $$
跟return差不多，当然也可以采用分数阶差分，但是变平稳有什么作用

2. 有没有可能其实生成的数量这么大也无所谓？



